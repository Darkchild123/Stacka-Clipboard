# ============================================================
# Stacka - requirements.txt
# ============================================================
# All external Python libraries Stacka needs to run.
#
# Install everything at once:
#   pip install -r requirements.txt
#
# ============================================================

# UI framework — replaces tkinter and pystray (May 2026 migration)
PyQt6==6.7.0

# Windows clipboard and system access
pywin32==306

# Image handling — opening, saving, resizing, thumbnails
Pillow==10.3.0

# Mouse position, simulating keyboard shortcuts (Ctrl+V)
pyautogui==0.9.54

# Global hotkey listener (Ctrl+Shift+V)
keyboard==0.13.5

# Getting app/process names for source tracking
psutil==5.9.8

# UI Automation (COM) — detecting in-page context menus (web/Electron
# apps) so the overlay button can position outside their borders
comtypes==1.4.16
